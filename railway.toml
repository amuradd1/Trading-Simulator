[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "/bin/sh -lc 'exec gunicorn server:app --bind 0.0.0.0:${PORT:-5000} --workers 1 --threads 4 --timeout 120'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
